<%= will_paginate %><br/>

<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= link_to user.username, user %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Delete User', user, :confirm => "Are you sure you'd like to delete #{user.username}?", :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= will_paginate %>
